version: '2'

services:
    grafana:
        image: grafana/grafana:latest
        ports:
            - 3000:3000
        volumes:
            - grafana-storage:/var/lib/grafana
            - ./workdir:/home/grafana

volumes:
    grafana-storage:
